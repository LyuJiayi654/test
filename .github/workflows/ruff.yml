name: format

on: 
  push:
   branches:
    - test
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4

    - name: format check
      run: bash shell.sh